apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "omni-tools.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "omni-tools.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "omni-tools.selectorLabels" . | nindent 6 }}
  replicas: {{ .Values.replicaCount | int }}
  template:
    metadata:
      labels:
        {{- include "omni-tools.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.securityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ include "omni-tools.name" . }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.targetPort }}
              protocol: TCP
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      restartPolicy: Always
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
