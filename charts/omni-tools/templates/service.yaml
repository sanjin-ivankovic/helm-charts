apiVersion: v1
kind: Service
metadata:
  name: {{ include "omni-tools.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "omni-tools.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "omni-tools.selectorLabels" . | nindent 4 }}
